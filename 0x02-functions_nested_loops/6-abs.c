#include "main.h"
/**
 * _abs - it returns the absolute value of an integer passed to it
 * @x: is the integer passed to the function
 * Return: The absolute value
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
