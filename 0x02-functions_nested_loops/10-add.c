#include "main.h"
/**
 * add - Adds two integers
 * Return: the result of the addition
 * @x: one of the two integers
 * @y: the second integer
 */

int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
